# Kotlin WASM Support

The Kotlin toolchain supports WASI (WebAssembly System Interface) via the Kotlin/Wasm compiler backend.
WASM support is active and compiled using standard Gradle targets (`wasmWasi`).